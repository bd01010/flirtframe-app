import SwiftUI

@main
struct SimpleFlirtFrameApp: App {
    var body: some Scene {
        WindowGroup {
            Text("FlirtFrame - Loading...")
                .font(.largeTitle)
                .padding()
        }
    }
}